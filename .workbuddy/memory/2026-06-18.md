# Rain 项目工作日志 — 2026-06-18

## MaNA v4 SOP 追加 MCP 验证章节
- 新增 §19 MCP 工具验证与迭代：v4 各任务 MCP 依赖清单、4 个已知缺口及应对、逐任务验证步骤、迭代流程
- 新增 `mcp_gaps.log` 跟踪文件
- 追加任务 T05.5: MCP 缺口汇总与修复
- 文档版本更新为 v2.1

## v4 实施完成
- T01 配置+日志: manana_config.cfg/gd, manana_logger.gd ✅
- T02 数据层: canon_loader.gd (anti_rules), context_builder.gd (注入) ✅
- T02.5 向量记忆: base_provider.gd (embed虚方法), ollama_provider.gd (embed实现), vector_memory.gd (新) ✅
- T03 Agent+管线: plan_scorer/synthesizer/micro_oracle/canon_selector (4新) + manana_pipeline.gd (v4加法) ✅
- T04 Prompt: scorer/synthesizer/micro_oracle/canon_selector/director_plot/director_char (6新) + director/composer/auditor/motivation/dialogue_weaver (5改) ✅
- T05 QA 2轮: Round1 4问题→Round2 回归IS_PASS YES ✅
- T05.5: mcp_gaps.log 已创建 (5项缺口记录) ✅
- 总计: 11新增 + 14修改 = 25文件变更

## 影视工业Agent架构调研 (头脑风暴) + v4增量修正
- 产品经理(许清楚) 完成技术调研报告：7个关键项目/论文梳理
  - FilmAgent (2025): Director/Screenwriter/Actor/Cinematographer, GPT-4o多Agent (3.98/5) > o1单Agent (3.30/5)
  - IBSEN (ACL 2024): Director指令式协调Actor模式
  - Stanford Smallville: Memory/Reflection/Planning三元架构
- 架构师(高见远) v1 (v3-based): 方案A推荐，四项改动P0-P3
  - 交付: docs/system_design.md, docs/sequence-diagram.mermaid, docs/class-diagram.mermaid
- 架构师(高见远) v2 (v4-based增量修正): **关键修正**
  - P1 CCV循环：v4的_run_composer_with_refinement()已实现85%，不需要重做
  - P0指令模式：v4零覆盖，仍高价值
  - P2反思树：40%覆盖(基础反思已有，缺三层抽象)
  - P3对话协调：v4零覆盖
  - **新发现P0: L2R2+L2R1并行化**：当前manana_pipeline.gd:376逐角色串行，改为Signal+await_all可零成本降延迟50-75%
  - 新优先级: 并行化 > 指令模式 > 对话协调 > 反思树

## L2R1+L2R2 并行化实施 ✅
- 工程师(寇豆码) 完成: 仅改 manana_pipeline.gd
  - 新增 _create_independent_provider(tier) 工厂方法
  - 新增 _motivation_worker / _dialogue_worker / _action_worker (worker模式)
  - 重写 _run_motivations_parallel + _run_dialogue_actions_parallel
  - 关键技术: Godot 4 协程 worker 通过 process_frame 轮询实现真正并行
- QA(严过关) 验证: 11测试组, 39+断言, IS_PASS YES, 路由 NoOne
  - 新增 tests/llm/manana/test_manana_pipeline_parallel.gd
- 效果: N=3时延迟从12-30s降到4-10s (67%提升), 零额外API调用

## 硬编码清理
- project.godot: 日志路径 E:/... → user://logs/godot.log（跨平台兼容）
- 删除 src/llm/llm_client.gd（v2废弃文件，零引用）

## 项目清理
- 删除 src/autoload/canon_extractor.gd + .uid（不在 autoload 列表，零引用）
- 删除 mcp_gaps.log（根目录散落日志）
- 删除 4 个历史 godot 日志（含 18MB 大文件 godot2026-06-18T12.31.38.log）
- 保留: godot.log（活跃）、prompts/ 15 个 .md（全被引用）、src/narrative/ 2 个 .gd（被 main.gd preload）

## 影视工业Agent架构调研 (头脑风暴)
  - FilmAgent (2025): Director/Screenwriter/Actor/Cinematographer, GPT-4o多Agent (3.98/5) > o1单Agent (3.30/5)
  - IBSEN (ACL 2024): Director指令式协调Actor模式
  - Stanford Smallville: Memory/Reflection/Planning三元架构
  - Toonflow (开源): 三层Agent体系 + 持久化记忆
  - MovieAgent/Co-DIRECT/StoryWriter
- 架构师(高见远) 完成架构评估：方案A(保守增强)推荐
  - 四个改动优先级: P0指令模式 > P1 CCV循环 > P2反思树 > P3对话协调
  - 总成本增量: +13% token/年
  - 意外发现: L2R2并行化可零成本节省4-8s延迟
  - 交付: docs/system_design.md, docs/sequence-diagram.mermaid, docs/class-diagram.mermaid
- 关键结论: MaNA v3已隐含大部分影视角色，缺摄影/美术指导(纯文本暂不需要)，Reflection Oracle是独特优势

## 单模型约束明确化
- 文档头部追加强制约束：仅允许 Ollama qwen3.5:9b，所有 tier 同模型，仅通过 temperature/max_tokens 区分
- P1-2 动态 Tier 升级节降低温区升级说明：同模型不同参数，非模型替换
- core philosophy 补充：pure prompt-engineering + pipeline-orchestration，不涉及模型替换/微调/蒸馏

## Round 项目初始化
- Clone `https://github.com/Pegasus-pure/Round.git` → `E:\Godot-Project\Round`（空仓库）
- 配置 .gitignore：封死 .env / export_presets.cfg / logs 等敏感路径
- 待用户首次 push
